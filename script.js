function redirectToYear(category) {
    window.location.href = `year.html?category=${category}`;
}
function redirectToSorry(category) {
    window.location.href = `sorry.html?
category=${category}`;
}
function redirectToExam(category) {
    window.location.href = `exam.html?
category=${category}`;
}
function redirectTo11finalsub(category) {
    window.location.href = `class11-final.html?
category=${category}`;
}
